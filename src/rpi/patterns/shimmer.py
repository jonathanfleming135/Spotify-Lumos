import arduino_control as arduino
import constants as const
import random

def pattern():
	print("hellotesthello")

	#led_write_time = 200
	#leds_per_write = arduino.max_leds_per_write(led_write_time)
#
	#leds = []
	#for led in range(0, leds_per_write):
	#	curr_led = random.randint(0, const.ALL_LEDS)
	#	if ((curr_led, const.WHITE) not in leds):
	#		leds.append((curr_led, const.WHITE))
#
	#print(leds)